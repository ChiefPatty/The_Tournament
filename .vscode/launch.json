{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Start The Tournament",
            "program": "${workspaceFolder}/./play",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "preLaunchTasks"
        }
    ]
}
